import styled from "styled-components";

export const StyledAnticipate = styled.div`
  display: flex;
  margin: 50px auto;
  width: max-content;
  justify-content: center;
  border: 1px solid var(--primary_text200);
  border-radius: 10px;
  background-color: var(--primary200);

  form {
    width: 400px;
  }
`;
